{% extends 'base.html.twig' %}

{% block content %}
  <div class="list-group">
    <a class="list-group-item list-group-item-action" href="{{ path('pulls') }}">{{ 'Pull requests'|trans }}</a>
    <a class="list-group-item list-group-item-action" href="{{ path('branches') }}">{{ 'Branches'|trans }}</a>
  </div>
{% endblock %}
